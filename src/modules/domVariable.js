const listScore = document.querySelector('.listNameAndScore');
const refreshButton = document.querySelector('.refresh');
const nameInputText = document.querySelector('.nameInput');
const scoreInputText = document.querySelector('.scoreInput');
const submitScore = document.querySelector('.btn-submit');

export {
  refreshButton,
  nameInputText, scoreInputText, submitScore,
};

export default listScore;